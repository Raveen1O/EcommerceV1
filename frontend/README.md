# Ecommerce Frontend

Minimal React frontend for the microservices backend.

Run:

```bash
cd frontend
npm install
npm start
```

Config: `src/aws-exports.js` contains the Cognito config and API base URL.
